/**
 * You can provide comments in npm-test.json
 *
 * When running on Travis CI the `codecov` is appended.
 *
 * When running `test-coverage` the Istanbul command is necessary; any options defined will be appended.
 *
 * If Babel or `typhonjs-npm-scripts-build-babel` is installed and configured uncomment `--compilers js:babel-register`
 * in the Mocha options below. Also if your test scripts are ES6 then use `babel-node` instead of `node` to launch the
 * testing NPM scripts.
 */
{
   "codecov": "&& cat ./coverage/lcov.info | ./node_modules/codecov.io/bin/codecov.io.js",

   "istanbul":
   {
      "command": "cover",

      "options": ["--report lcovonly"]
   },

   "mocha":
   {
      "source": "./test-src/src",

      "options":
      [
         // "--compilers js:babel-register",
         "-t 120000 --recursive"
      ]
   }
}